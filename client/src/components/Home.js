import React from 'react';
import '../App.css';

const Home = () => {
	return (
		<div>
			<p>
				This is a simple example of using React to Query the Marvel API. Start by clicking the "Characters", "Comics", and "Stories" button above
			</p>
		</div>
	);
};

export default Home;
